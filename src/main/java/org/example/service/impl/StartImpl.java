package org.example.service.impl;

import ch.qos.logback.classic.Logger;
import org.slf4j.LoggerFactory;

public class StartImpl {
    private static final Logger log = (Logger) LoggerFactory.getLogger(StartImpl.class);
}
